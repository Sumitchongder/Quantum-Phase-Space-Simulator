# ── CV Quantum Information Dashboard ── requirements_hpc.txt ──────────────────
# HPC / local machine dependencies for running quantum computations
# Run: pip install -r requirements_hpc.txt
# Then: python generate_data.py
# Platform: IIT Jodhpur HPC (cn03) · Python 3.11 · linux/amd64
# DO NOT install these on Streamlit Cloud — use requirements.txt instead

# ── Core scientific stack ──────────────────────────────────────────────────────
numpy>=1.24,<2.0
scipy>=1.11,<2.0

# ── Quantum framework ─────────────────────────────────────────────────────────
# NOTE: QuTiP 5.x requires numpy<2.0 — pinned above
qutip>=5.0,<6.0

# ── Visualization (notebooks) ─────────────────────────────────────────────────
matplotlib>=3.7,<4.0
plotly>=5.18,<6.0

# ── Data ──────────────────────────────────────────────────────────────────────
pandas>=2.0,<3.0

# ── Jupyter ───────────────────────────────────────────────────────────────────
jupyter>=1.0
notebook>=7.0
ipykernel>=6.0

# ── GBS — Gaussian Boson Sampling ─────────────────────────────────────────────
strawberryfields>=0.23,<0.24
thewalrus>=0.21,<0.22

# ── CV-QML ────────────────────────────────────────────────────────────────────
pennylane>=0.36,<0.43
pennylane-sf>=0.30

# ── Dashboard (optional — to test locally) ────────────────────────────────────
streamlit>=1.32,<2.0
